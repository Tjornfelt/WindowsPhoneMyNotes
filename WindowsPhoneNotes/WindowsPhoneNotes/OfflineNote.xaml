<Page
    x:Class="WindowsPhoneNotes.OfflineNote"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsPhoneNotes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="5">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="20*" />
            <RowDefinition Height="3*" />
        </Grid.RowDefinitions>
        
        <TextBlock Text="Offline note" VerticalAlignment="Stretch" Grid.Row="0" FontSize="42" Foreground="White" SelectionHighlightColor="#FF7954D1"></TextBlock>
        <TextBox Name="notebody" BorderThickness="0" Grid.Row="1" FontSize="26" Foreground="Black" TextWrapping="Wrap" VerticalAlignment="Stretch" Background="White" />
        <Button Click="back_Click" Name="back" Background="red" Grid.Row="3" BorderThickness="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">Exit</Button>
    </Grid>
</Page>
