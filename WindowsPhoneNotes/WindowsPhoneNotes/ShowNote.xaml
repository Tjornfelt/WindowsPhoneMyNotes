<Page
    x:Class="WindowsPhoneNotes.ShowNote"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsPhoneNotes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="5">

        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="10*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <TextBlock Name="noteheader" Grid.Row="0" FontSize="42" Foreground="White" SelectionHighlightColor="#FF7954D1">test</TextBlock>
        <TextBlock Name="notebody" Grid.Row="1" FontSize="26" Foreground="White" TextWrapping="Wrap">test</TextBlock>
        <Button Click="back_Click" Name="back" Background="red" Grid.Row="2" BorderThickness="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">Back</Button>
    </Grid>
</Page>
